# 开发版交接

## 当前分支
`develop`，目录 `D:\tools\steam-market-auto-lister-dev`。已推送至 `origin/develop`。

## 最新提交
`c9cf2f5 按资产ID精确匹配市场挂单`

## 已完成内容
- 修复同一资产重新上架且价格变化时被误判为外部挂单的问题。
- 本地提交数量多于 Steam 在售数量时，释放多出的记录并标记提交为 `sold`，不再重复计入。
- 外部挂单保留状态跟踪，不参与自动调价，也不计入完整运行失败。
- 价格同步失败或目标物品没有可用价格历史时，逐项报告并停止生成/提交本次新上架计划；已有在售挂单不受影响。
- 按 `(appid, contextid, assetid)` 精确匹配 Steam 返回的资产，避免同款不同资产 ID 被错误关联。
- 相关测试通过：`67 passed`；Ruff、前端语法检查和 `git diff --check` 均通过。

## 未完成事项
无。本任务不再扩展功能。

## 已知问题
- Steam 页面/API 变化可能导致库存、在售或价格解析失败。
- 外部挂单没有自动调价时间；只有识别为程序历史提交后才会接入策略。
- 本地数量少于 Steam 数量时，多出的 Steam 挂单仍显示为外部。

## 关键配置
配置前缀为 `STEAM_LISTER_`。真实市场写入需同时设置 `DRY_RUN=false` 与 `ALLOW_MARKET_WRITES=true`。价格和市场操作依赖已登录 Steam、网络代理及正确钱包币种。

## 下一步建议
重启开发版服务后运行一次“同步当前在售”，再执行 `run-full-once.bat`；若价格同步失败，先检查 Steam 登录状态、代理和行情接口。

## 2026-08-03 修复同款资产错误匹配

- 原因：此前同款挂单按数量顺序匹配，Steam 的另一件同名饰品可能被错误分配给仍在库存的资产，导致库存物品被标记为 `active/matched` 而跳过上架。
- 修复：Steam 返回资产 ID 时，按 `(appid, contextid, assetid)` 精确匹配；只有资产 ID真实对应的本地程序提交才标记为 `matched`，其他 Steam 挂单标记为外部，未匹配的库存资产释放后可重新生成计划。
- Steam 未返回资产 ID 时保留旧的分组兜底逻辑。
- 新增同款不同资产 ID的回归测试。
- 验证：全量测试 `67 passed`，相关 Ruff、前端语法检查和 `git diff --check` 均通过。
